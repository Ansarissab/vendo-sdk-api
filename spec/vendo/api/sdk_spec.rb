# frozen_string_literal: true

RSpec.describe Vendo::Api::Sdk do
  it "has a version number" do
    expect(Vendo::Api::Sdk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
